import pygame as pg
import sys
# Import the Pygame library and sys module for system functions
# from pygame.locals import QUIT
# # Import QUIT constant from pygame.locals for event handling


pygame.init()
# Initialize the Pygame library


# screen = pg.display.set_mode((800, 600))
# # Set the display mode to 800x600 pixels
# pg.display.set_caption("Pygame Window")
# # Set the window title to "Pygame Window"
# running = True
# # Main loop to keep the window open
# while running:
#     for event in pg.event.get():
#         if event.type == pg.QUIT:
#             running = False
#             # Exit the loop if the window is closed

#     screen.fill((0, 0, 0))
#     # Fill the screen with black color
#     pg.display.flip()
#     # Update the display
# pygame.quit()
# # Quit Pygame when done
